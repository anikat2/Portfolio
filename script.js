function scrollToSkills() {
    var skillsSection = document.getElementById('skills');
    skillsSection.scrollIntoView({ behavior: 'smooth' });
}

function scrollToProjects() {
    var skillsSection = document.getElementById('proj');
    skillsSection.scrollIntoView({ behavior: 'smooth' });
}